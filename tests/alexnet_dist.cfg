configuration AlexNet_Dist is
   pragma Starter (None);
   pragma Boot_Location ("tcp", "localhost:4130");
   Server : Partition := (AlexNet);
   Client : Partition := ();
   procedure AlexNet_Run is in Client;
end AlexNet_Dist;
